@model CultureITS.Models.UserLogin

@{
    ViewBag.Title = "Авторизация";
    ViewBag.BreadCrumbs = new CultureITS.Helpers.BreadCrumbsBuilder().Generate(ViewBag.Title);
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(false)

    <div class="cont">
        <div class="column2">
            <h3 class="headline">Учётные данные</h3>

            <div class="editor-label">
                @Html.LabelFor(model => model.Login)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => model.Login)
                @Html.ValidationMessageFor(model => model.Login)
            </div>

            <div class="editor-label">
                @Html.LabelFor(model => model.Password)
            </div>
            <div class="editor-field">
                @Html.PasswordFor(model => model.Password)
                @Html.ValidationMessageFor(model => model.Password)
            </div>

            <p>
                <button class="button">Выполнить вход</button>
            </p>
        </div>
        <div class="column2">
            <h3 class="headline">????</h3>

            <p>Введите ваши учётные данные в поля, расположенные на данной странице.</p>
            <p>Если Вы по каким-либо причинам не можете выполнить вход, проверьте следующие условия:</p>
            <ol>
                <li>Вы набираете пароль в правильной раскладке;</li>
                <li>Клавиша <span class="button">Caps Lock</span> находится в выключенном состоянии.</li>
            </ol>
            <p>Если несмотря на это Вы не можете авторизоваться, обратитесь к администратору системы.</p>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
